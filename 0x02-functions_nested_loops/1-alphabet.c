#include "main.h"
/**
 * print_alphaber - function for printing lowercase alphabets
 */
void print_alphabet(void);
{
	char alpha = 'a';

	while (alpha <= 'z')
	{
		_putchar(apha);
		alpha++
	}

	_putchar(10);
}
